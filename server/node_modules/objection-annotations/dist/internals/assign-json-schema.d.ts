import { Model } from 'objection';
import { ColumnOpts } from '../declarations';
export declare function resolveColumnAnnotations<T>({ type, opts }: {
    opts: ColumnOpts;
    type: string | (() => [T | (() => T), string]);
}, forceType?: boolean): {
    type?: undefined;
    opts?: undefined;
} | {
    type: string;
    opts: ColumnOpts;
};
export declare function assignJsonSchema(target: typeof Model): void;
