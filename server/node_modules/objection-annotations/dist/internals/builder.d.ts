import Knex = require('knex');
import { Model } from 'objection';
import { ColumnOpts, ForeignKeyInfo, TableForeignInfo } from '../declarations';
interface ColumnSchema {
    opts: ColumnOpts;
    type: string;
}
export declare function buildForeignKey(builder: Knex.TableBuilder, { foreignKeyName, alterSource, source, target }: ForeignKeyInfo): Knex.ColumnBuilder;
export declare function secondBuilderParam({ opts, type }: ColumnSchema): number | "text" | "tinytext" | "mediumtext" | "longtext";
export declare function buildTableFromBuilder<T extends typeof Model>(builder: Knex.TableBuilder, modelClass: T, refs: TableForeignInfo): void;
export {};
